<% include ./partials/header %>

<div class="container">
    <div class="row">
        <div class="col-lg-8 justify-content-center">
            <h1>HOME</h1>
            <% games.forEach(function(game, i){ %>
                <a href="/game/<%= i %>" class="btn btn-secondary">Go to <%= game.name %></a>
            <% }); %>
        </div>
    </div>
</div>

<% include ./partials/footer %>